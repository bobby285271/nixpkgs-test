on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: cachix/install-nix-action@v17
      - run: nix-build -A cargo-public-api https://github.com/NixOS/nixpkgs/archive/f8e8a85cbafb0d5e1f5ffb51d43398d693bd3e3b.tar.gz
