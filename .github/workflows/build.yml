on:
  push:
    branches:
      - main
  workflow_dispatch:
  
  
env:
  PACKAGE_ATTR: pkgsMusl.glib
  MAIN_PROGRAM: foo
  PR_NUMBER: c9410b118cd576a189cac7705acc4b412cb98d44

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: cachix/install-nix-action@v17
      - run: nix-build -A ${PACKAGE_ATTR} https://github.com/NixOS/nixpkgs/archive/${PR_NUMBER}.tar.gz
      - run: ls -lA
