on:
  push:
    branches:
      - main
  workflow_dispatch:
  
  
env:
  PACKAGE_ATTR: pkgsMusl.glib
  MAIN_PROGRAM: foo
  PR_NUMBER: ab8a0ada6c6f59a6391065ed650155a910d7a1db

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: cachix/install-nix-action@v17
      - run: nix-build -A ${PACKAGE_ATTR} https://github.com/NixOS/nixpkgs/archive/${PR_NUMBER}.tar.gz
      - run: cd ./result-bin/bin && ls
