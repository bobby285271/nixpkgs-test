on:
  push:
    branches:
      - main
  workflow_dispatch:
  
  
env:
  PACKAGE_ATTR: adwaita-qt
  COMMIT_SHA: 4ccb965fd0b406e3630a8550a2d344f295f0c5a0
jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: cachix/install-nix-action@v17
#         with:
#           extra_nix_config: |
#             sandbox = false
      - run: nix-build -A ${PACKAGE_ATTR} https://github.com/NixOS/nixpkgs/archive/${COMMIT_SHA}.tar.gz
      - run: ls -lA
